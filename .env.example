# OpenRouter configuration (REQUIRED)
# Sign up at https://openrouter.ai and get your API key
OPENROUTER_API_KEY=

OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_NAME=ai-testbot

# LLM Mode: balanced (recommended) | cheap | premium
LLM_MODE=balanced

# ===== STABLE PAID MODELS (Recommended) =====
# These models are reliable and cost-effective (~$0.20-1.00 per run)
# NO FREE MODELS - free models have rate limits and unreliable availability

# High-quality planning and strategy (most expensive but best reasoning)
# Cost: ~$0.50 per run | Use for: complex analysis, test strategy
LLM_MODEL_PLANNER=anthropic/claude-3.5-sonnet

# Code generation and test writing (best quality/cost ratio)
# Cost: ~$0.10 per run | Use for: generating tests, fixing code
LLM_MODEL_CODER=qwen/qwen-2.5-coder-32b-instruct

# Long context (100k+ tokens) for architecture analysis
# Cost: ~$0.30 per run | Use for: large codebases, full repo analysis
LLM_MODEL_LONG_CONTEXT=meta-llama/llama-3.3-70b-instruct

# Quick transformations and simple tasks
# Cost: ~$0.05 per run | Use for: formatting, simple fixes
LLM_MODEL_HELPER=qwen/qwen-2.5-coder-32b-instruct

# Primary fallback model (if specific model fails)
OPENROUTER_MODEL=qwen/qwen-2.5-coder-32b-instruct

# Secondary fallback (if primary also fails)
OPENROUTER_MODEL_FALLBACK=meta-llama/llama-3.1-8b-instruct

# Token budget (increase if running on large repos)
LLM_MAX_TOKENS_PER_RUN=250000
LLM_TOKEN_WARN_THRESHOLD=0.8


